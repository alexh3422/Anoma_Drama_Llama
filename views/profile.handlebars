{{#if currentUserMood}}
<div class="column {{currentUserMood}}Border">
 <div class="moodSelector {{currentUserMood}}">
{{else}}
<div class="column">
 <div class="moodSelector default">
  {{/if}}
    {{> mood-wheel}}
  </div>

  <div class="allPosts">
    <h2>Your Drama</h2>
    {{#each userPosts}}
    <div class="post-box">
      <p id="postUsername">Welcome {{user.username}}! Here is all your drama:</p>
      <p id="title">"{{title}}"</p>
      <p id="text">{{text}}</p>
      <p>You felt {{moodText}} on {{dateFormat createdAt}}</p>
    </div>
    {{/each}}
  </div>
</div>

<script src="/js/currentMood.js"></script>